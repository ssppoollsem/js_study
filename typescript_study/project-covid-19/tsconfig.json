{
  "compilerOptions": {
    "allowJs": true,
    "target": "ES5",
    "outDir": "./built",
    "moduleResolution": "Node",
    "lib": ["es2015", "DOM", "DOM.Iterable"],
    "noImplicitAny": true // 타입을 지정하지 않는 경우 any라도 추가해야한다.
  },
  "include": ["./src/**/*"], // 해당폴더에 있는 파일들만 컴파일하겠다.
  "exclude": ["node_modules", "dist"]
}
