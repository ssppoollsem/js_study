import Layout from './Layout';
import { PageHeader } from 'antd';

export default function List() {
    return (
        <Layout>
            <PageHeader title={} />
        </Layout>
    );

    function click() {}
}
